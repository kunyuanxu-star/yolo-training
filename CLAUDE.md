# 项目约束

## 提交信息（Commit Message）约束

写 commit message 时，遵循以下规则：

1. **只写一行**，不要写 body / 多行说明。
2. **不废话**：说出这次改了什么即可，不要背景、不要规划、不要语气词。
3. 遵循本仓库的既有格式：`类型(范围): 一句话`（参照历史 commit，如 `fix: ...`、`refactor: ...`）。
4. 如果一行无法讲清，选最核心的一个改动点概括，不要罗列全部细节。